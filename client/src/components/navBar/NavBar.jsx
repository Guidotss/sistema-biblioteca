import { Form } from "../barrel"
import "./navBar.css"

export const NavBar = () => {
  return (
    <header>
        <h1>Biblioteca</h1>
        <nav>
            <Form/>
        </nav>
    </header>
  )
}